# **Цель работы**

Научиться создавать Dockerfile, понимать различия между «плохими» и
«хорошими» практиками при написании Dockerfile, а также освоить базовые
принципы работы с контейнерами.

------------------------------------------------------------------------

# **Ход работы**

## **1. АэАЭАЭАЭ начинаем понимать суть и читать**
![Без названия](https://github.com/user-attachments/assets/53bab3c7-9a1c-4445-8ad1-5b3f0880df0e)

Первое, что нужно было сделать — установить Docker Desktop и проверить,
что команда docker работает в PowerShell.
После успешной установки началась битва с первым Dockerfile — тем самым
плохим, который специально содержит ошибки и антипаттерны.

------------------------------------------------------------------------

## **2. Создание плохого Dockerfile**

```dockerfile
# Очень плохой 👿

FROM python:latest
# Ошибочка нет фиксированной версии образа(
WORKDIR /app
ADD . /app
# Ошибочка использование ADD вместо COPY (ужас как так можно)
RUN pip install flask requests pyyaml
# Ошибочка установка без requirements.txt и версий (мдээээ)

CMD ["python", "app.py"]
# Ошибочка: запуск от root (без указания USER, оооооо нннеееетттт)
```

Ошибки здесь очевидны:
- Используется большой образ Ubuntu
- Несколько команд RUN
- Использование ADD

------------------------------------------------------------------------

## **3. Исправленный хороший Dockerfile**
```dockerfile
# хорошенький 😇😇😇
FROM python:3.9.6  
#  Мы поменяли) стала конкретная версия образа

WORKDIR /app

# Мы поменяли) COPY вместо ADD пупупуппу
COPY requirements.txt ./

# Мы поменяли) 
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Мы поменяли) теперь безопасный запуск
RUN useradd -m appuser && \
    chown -R appuser:appuser /app
USER appuser

EXPOSE 5000
CMD ["python", "app.py"]
```
![Без названия (1)](https://github.com/user-attachments/assets/59e33da7-3515-4fb3-8a0f-c0c363ccc647)
------------------------------------------------------------------------

## **4. Flask‑приложение**
```dockerfile
app.py

    from flask import Flask, jsonify

    app = Flask(__name__)

    @app.route('/')
    def home():
        return jsonify({"message": "Hello from container!"})

    if __name__ == '__main__':
        app.run(host='0.0.0.0', port=5000)
```

requirements.txt

    Flask==2.0.3
    Werkzeug==2.0.3
    requests==2.28.1
    PyYAML==6.0

------------------------------------------------------------------------

## **5. Сборка и запуск контейнеров**
```dockerfile
    docker build -f Dockerfile.bad -t lab-bad .
    docker build -f Dockerfile -t lab-good .
    docker run -p 5000:5000 lab-good
```
Результат:
{"message": "Hello from container!"} УУУУУРРРРРРРРААААААА 🥳🥳🥳

------------------------------------------------------------------------

## **6. Отличия**

  Проблема                 Плохой Dockerfile   Хороший Dockerfile
  ------------------------ ------------------- --------------------
  Базовый образ            ubuntu:latest       python:3.9.6
  Слои                     Несколько RUN       Один RUN
  Копирование              ADD . .             COPY . .
  Установка зависимостей   inline              requirements.txt

------------------------------------------------------------------------

## **7. Ошибки при работе**

-   Запуск контейнера от root
-   Отсутствие лимитов ресурсов

------------------------------------------------------------------------

## **8. Результаты**

-   изучены принципы Dockerfile;
-   созданы два образа;
-   Flask‑приложение запущено;
-   оптимизированный образ легче и безопаснее.

------------------------------------------------------------------------

# **Вывод:**

В ходе лабораторной работы удалось освоить базовые принципы работы с
Docker.
После всех тестов контейнер заработал, а сообщение Hello from
container! стало самым приятным результатом 😄
![Без названия (2)](https://github.com/user-attachments/assets/1c74e223-3b28-4a6b-acbe-4ec9fb9f0072)



